import "../styles/side-bar.css";

export const Sidebar = () => {
    return (
        <nav>
            <div>POKEMONS</div>
            <div>BERRIES</div>
            <div>ITEMS</div>
        </nav>
    );
}